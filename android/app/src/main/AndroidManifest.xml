<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Audio capture + routing -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Incoming-call ringtone vibration (InCallManager.startRingtone's
         vibrate_pattern argument) — a normal manifest permission, not a
         runtime/dangerous one, so no request flow is needed. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Contacts tab -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Keep the call alive when the app is backgrounded -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Phase 3a POC — SipKeepAliveService. Required in addition to the base
         FOREGROUND_SERVICE permission above for the specialUse type. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Turn the screen on for an incoming call -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <uses-feature android:name="android.hardware.microphone" android:required="true" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:showWhenLocked="true"
        android:turnScreenOn="true"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Lets other apps hand tel: links to EgoVoip -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <action android:name="android.intent.action.DIAL" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="tel" />
        </intent-filter>
      </activity>

      <!-- Phase 3a POC — empirically testing whether a correctly-typed
           foreground service keeps the existing JsSIP WebSocket/REGISTER
           alive through background/lock/swipe-away. Not the final production
           architecture (see ARCHITECTURE.md) — specifically, this does not
           yet cover Core-Telecom/CallStyle/full-screen-intent, which govern
           whether an incoming call is presented well on a *locked* screen;
           this phase only tests whether the SIP session itself survives. -->
      <service
        android:name=".SipKeepAliveService"
        android:exported="false"
        android:foregroundServiceType="specialUse">
        <property
          android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
          android:value="Maintains SIP registration so the app can receive incoming VoIP calls while backgrounded" />
      </service>
    </application>
</manifest>
